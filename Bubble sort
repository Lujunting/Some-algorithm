#bubble sort

def bubble_sort(m):
    swap=False
    for k in range(len(m)-1):
        if m[k]>m[k+1]:
            m[k],m[k+1]=m[k+1],m[k]  # 3,2 m[k],m[k+1]--> m[k+1],m[k] 2,3
            swap=True
    if swap==True:
        m=bubble_sort(m)          
    return m 
print(bubble_sort([3,2,4,5,1]))
